const aluminumTransformer = extend(ConsumeGenerator, "aluminum-transformer", {});

const ventGenerator = extend(ThermalGenerator, "vent-generator", {});

const powerNode = extend(PowerNode, "power-node", {});
const powerNodeLarge = extend(PowerNode, "power-node-large", {});